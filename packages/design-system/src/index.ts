/**
 * @safe-global/design-system
 *
 * Design system tokens and utilities for Safe{Wallet}.
 * Tokens are synced from Figma using the CLI: `yarn sync-tokens`
 */

export type {
  DesignToken,
  TokenCollection,
  TypographyToken,
  TokenManifest,
} from './types/tokens'
