import colorsTokens from './colors.tokens.json'
import spacingTokens from './spacing.tokens.json'
import radiusTokens from './radius.tokens.json'

export const baseTokens = {
  colors: colorsTokens,
  spacing: spacingTokens,
  radius: radiusTokens,
}

export { colorsTokens, spacingTokens, radiusTokens }
